const {resolve} = require('path');

/** @type {string} Path to the partials folder */
exports.partials = resolve(__dirname, './partials');

/** @type {string} Path to the helpers folder */
exports.helpers = resolve(__dirname, './helpers');