{
	"name": "ubc-farm-server-proxy",
	"version": "1.1.2",
	"description": "Reverse proxy parent server that directs requests to the correct subservers. Allows for easier code splitting, as well as the potential for certain functions to be moved to seperate systems in the future",
	"author": "Tiger Oakes <tigeroakes@gmail.com> (http://tigeroakes.com)",
	"license": "MIT",
	"repository": "https://github.com/NotWoods/ubc-farm/tree/master/packages/ubc-farm-server-proxy",
	"main": "index.node.js",
	"jsnext:main": "index.es.js",
	"private": true,
	"dependencies": {
		"hapi": "14.1.0",
		"h2o2": "5.1.0"
	},
	"devDependencies": { 
		"rollup": "0.34.7",
		"rollup-plugin-json": "^2.0.1" 
	},
	"scripts": {
		"prepublish": "rollup -c",
		"start:api": "cd ../ubc-farm-api && npm run prepublish && npm start",
		"start:static": "cd ../ubc-farm-static-assets && npm run prepublish && npm start",
		"start:package": "cd ../ubc-farm-package-server && npm run prepublish && npm start",
		"start:views": "cd ../ubc-farm-views-server && npm run prepublish && npm start",
		"start": "start npm run start:api && start npm run start:static && start npm run start:package && start npm run start:views",
		"poststart": "node index.node.js",
		"quickstart": "npm run prepublish && npm start"
	}
}